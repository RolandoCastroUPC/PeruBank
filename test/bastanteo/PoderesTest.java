package bastanteo;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;

import BLL.AdmPoderes;
import BLL.ClienteException;
import ENT.Poder;

public class PoderesTest {

	@Test
	public void poderDeberiaCrearseConDatosIniciales() throws ClienteException{
		//	Preparamos el ambiente
		/*String codigoCorto = "CHCO";
		String nombre = "Cobro Cheques";
		String productoServicio = "Activo";
		AdmPoderes adm = new AdmPoderes();
		
		// Ejecutamos
		adm.registrarPoder(codigoCorto, nombre, productoServicio);
		
		Poder poder = adm.buscarPoder(codigoCorto);
		
		//Verificamos
		assertNotNull(codigoCorto);
		assertNotNull(nombre);
		assertNotNull(productoServicio); */
	}
}
